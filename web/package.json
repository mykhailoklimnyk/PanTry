{
  "name": "komora-web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json --tsgo && tsc --noEmit --project tsconfig.e2e.json",
    "deploy": "wrangler deploy",
    "test:e2e": "playwright test",
    "check:e2e": "tsc --noEmit --project tsconfig.e2e.json"
  },
  "//typescript": "Перевірку типів виконує TypeScript 7 (нативний Go-компілятор) через прапорець --tsgo. svelte-check вимагає, щоб поряд лежав і 6.x — це його власна умова роботи з сімкою, а не відкат версії: тип-чек іде на 7.0.2. Прибрати пакет typescript@6, щойно svelte-check перестане його вимагати.",
  "//fonts": "Manrope і IBM Plex Mono з дизайн-пакета, але локально, а не з Google Fonts: публічне демо не має залежати від чужого CDN і лишати запит на третій бік при кожному показі. Обидва шрифти під OFL — gen_licenses.py бере їх із package-lock.json.",
  "dependencies": {
    "@fontsource-variable/manrope": "^5.2.9",
    "@fontsource/ibm-plex-mono": "^5.2.9"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "@types/node": "^26.2.0",
    "@typescript/native": "npm:typescript@^7.0.2",
    "svelte": "^5.56.9",
    "svelte-check": "^4.7.5",
    "typescript": "^6.0.3",
    "vite": "^8.2.1",
    "wrangler": "^4.122.0"
  }
}
